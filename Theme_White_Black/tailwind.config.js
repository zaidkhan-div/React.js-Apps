export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
  darkMode: 'media',
}

// 'media' → If you want automatic OS-based dark mode (simpler)

// 'class' → If you need manual toggle control (more flexible)