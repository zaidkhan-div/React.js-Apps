// 🔥 Must-Learn (Used in 90% of Projects)
// Sorting (getSortedRowModel)

// Lets users sort columns (↑/↓). Basic table functionality.

// Pagination (getPaginationRowModel)

// Splits data into pages (e.g., "1 of 5"). Critical for large datasets.

// Row Selection (enableRowSelection)

// Checkboxes for selecting rows. Needed for bulk actions.

// Column Filtering (getFilteredRowModel)

// Filter data per column (e.g., status = "Active").

// 💡 Important (Used in 50% of Projects)
// Global Filtering (globalFilter, getFilteredRowModel)

// Search across all columns (like a search bar).

// Column Visibility (enableHiding)

// Show/hide columns (e.g., for user preferences).

// Column Sizing (enableColumnResizing)

// Drag to resize column widths. Useful for wide content.

// 🛠 Niche (Used in Specific Cases)
// Expanding (getExpandedRowModel)

// Nested sub-rows (e.g., order details).

// Grouping (getGroupedRowModel)

// Group rows by category (e.g., "Users by Country").

// Pinning (Column/Row)

// Freeze columns/rows (e.g., sticky headers).

// ⚡ Skip Initially (Advanced/Edge Cases)
// Virtualization (for 10,000+ rows)

// Faceting (complex filtering for analytics)

// Fuzzy Filtering (typo-tolerant search)

